# Supabase — the ladder, ghosts and match history.
# Without these the app still runs: solo + private-room play work fully offline.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
